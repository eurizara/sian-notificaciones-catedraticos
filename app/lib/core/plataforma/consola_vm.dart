/// Implementación para la máquina virtual: pruebas y herramientas.
///
/// `print` basta aquí; no hay consola de navegador que ocultar nada.
library;

// ignore: avoid_print
void consolaError(String mensaje) => print(mensaje);
