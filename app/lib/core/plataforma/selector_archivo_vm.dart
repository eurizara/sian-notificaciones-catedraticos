/// Implementación para la máquina virtual: pruebas y herramientas.
///
/// Devuelve `null`, que es lo mismo que devolvería alguien que abre el
/// selector y lo cierra sin elegir nada. Quien llama ya tiene que contemplar
/// ese caso, así que no hace falta un camino aparte.
library;

import 'archivo_elegido.dart';

Future<ArchivoElegido?> elegirImagen() async => null;
